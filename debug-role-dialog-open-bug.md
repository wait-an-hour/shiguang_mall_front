# [OPEN] 角色管理弹窗打不开

## 症状
- 角色管理页中“新增角色”和“编辑”点击后，弹窗不出现。

## 当前环境
- 前端工程：`web`
- 页面：`web/src/views/admin/RoleManageView.vue`

## 可证伪假设
1. 点击事件没有真正触发，`openCreate` / `openEdit` 没有执行。
2. 事件触发了，但 `dialogVisible` 没有被正确置为 `true`。
3. `el-dialog` 已经打开，但被层级 / 遮罩 / 容器样式挡住了，视觉上像没打开。
4. 角色页模板里仍有隐藏的运行时错误，导致按钮点击后的响应链被中断。
5. 权限树或表格渲染过程中有异常，阻止了后续弹窗渲染。

## 调试目标
- 先确认点击是否触发，再确认状态是否变化，最后确认弹窗是否渲染。
